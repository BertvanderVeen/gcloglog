% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile.phi.R
\name{profile.gcloglog}
\alias{profile.gcloglog}
\title{MLE estimation of the shape of a binomial link function}
\usage{
\method{profile}{gcloglog}(
  model,
  CI = TRUE,
  alpha = 0.05,
  plot = TRUE,
  h = 0.02,
  ytol = 2,
  ystep = 0.1,
  maxit = 100,
  adaptive = TRUE,
  trace = TRUE,
  ...
)
}
\arguments{
\item{model}{a model object which accepts a family argument (should correspond to "binomial") with a link function. The model should have corresponding \code{"\link{update}"} and \code{"\link{logLik}"} functionality implemented.}

\item{CI}{logical, defaults to \code{TRUE}.}

\item{alpha}{the confidence level, defaults to 0.05.}

\item{plot}{logical, defaults to \code{TRUE}, so that a plot of the profiling is displayed. Only when CI is set to \code{TRUE}.}

\item{h}{initial adaptive stepsize.}

\item{ytol}{Adjusts the range of the likelihood values.}

\item{ystep}{Adjusts the reoslution of the likelihood profile.}

\item{maxit}{Maximum number of iterations for the adaptive algorithm.}

\item{adaptive}{logical, defaults to \code{TRUE}. Implements adaptive step size.}

\item{trace}{logical, defaults to \code{TRUE}. Prints progress.}

\item{...}{other arguments passed to \code{"\link{profile.phi}"}.}
}
\value{
A list including the optimisation results, CI, and final model fit.
}
\description{
This function takes a model object, estimates a parameter phi that controls the shape of the link function, and performs further profiling for retrieving a confidence interval. The implementation heavily borrows from \code{"\link{TMB::tmbprofile}"}.
}
\examples{
# Define logit link via gcloglog
library(gcloglog)
gcloglog1 <- make.gcloglog(1)

# Profile the gcloglog link
x <- rnorm(100)
b1 = 2.3
b0 = 0.1
eta = b0+b1*x
y = rbinom(length(x), 1, plogis(eta))
data <- data.frame(y = y,x = x)
model <- glm(y~x, family = binomial(link = gcloglog1), data = data)
res <- profil.gcloglog(model)

final.model <- res$final.model
}
\references{
van der Veen and Hui (2025). In prep.
}
\author{
Bert van der Veen
}
